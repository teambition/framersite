import math

class VerticalAlgorithm(object):

    def process(self, sprite):
        y = 0
        for image in sprite.images:
            image.x = 0
            image.y = y
            y += image.absolute_height

            if "2x" in image.path and image.absolute_height % 2 > 0:
            	print image.path, image.absolute_height

        #     print image.absolute_height, image.path
        # print y
        # # y += y % 2